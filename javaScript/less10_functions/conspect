FUNCTION DECLARATION:
    Существует до того как она объявлена
        function foo() {
            код
        }

FUNCTION EXPRESSION:
    Функциональное выражение
    let foo = function() {
        Код
    };
    Создаеться только тогда, когда доходит поток кода, можно вызвать только после объявления.

Стрелочная функция:
    () =>
    Не имеет контекста вызова (this)